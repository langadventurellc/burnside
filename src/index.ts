/**
 * LLM Bridge Library
 *
 * A unified bridge to multiple LLM provider APIs for use across
 * Desktop (Electron), Mobile (React Native), and Web (Node.js) platforms.
 *
 * This is the main public API surface entry point.
 * Future exports will include the primary client interface and configuration types.
 */

// TODO: Export main client interface when implemented
// TODO: Export configuration types when implemented
// TODO: Export core types for public consumption when implemented

export {};
