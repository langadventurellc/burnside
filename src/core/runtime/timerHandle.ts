/**
 * Timer Handle Type
 *
 * Platform-agnostic timer handle type for timer operations.
 * Represents the return value from setTimeout/setInterval that can be
 * passed to clearTimeout/clearInterval across different platforms.
 */

/**
 * Timer handle type for platform-agnostic timer operations.
 * Represents the return value from setTimeout/setInterval that can be
 * passed to clearTimeout/clearInterval.
 */
export type TimerHandle = unknown;
