/**
 * Composite key type for provider identification
 *
 * Uses format "id:version" to uniquely identify a specific
 * version of a provider plugin in the registry storage.
 */
export type ProviderKey = string;
